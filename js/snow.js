(function () {
  // reference particlesTexture.html
  // basic variables
  var scene, camera, renderer, container;

  var stats;

  var clock;

  // snow stuff
  var snowGeometry, snowMaterial, snowMesh;

  function init() {
    container = document.getElementById('container')
  }
})();
